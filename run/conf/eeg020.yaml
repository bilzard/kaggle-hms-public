defaults:
  - baseline_efficientnet
  - _self_

exp_name: eeg020
description: eeg016 + channel mixer (inverted res. conv)

architecture:
  model:
    eeg_encoder:
      se_after_dw_conv: true
      use_channel_mixer: true
      channel_mixer_kernel_size: 3
      mixer_type: ir
