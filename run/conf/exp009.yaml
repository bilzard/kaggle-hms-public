defaults:
  - baseline
  - override architecture/model/head: label_aligned_head_v2
  - _self_

exp_name: exp009
description: label-aligned head v2 + nonlinearity

architecture:
  model:
    head:
      nonlinearity: True
      kernel_size: 3
