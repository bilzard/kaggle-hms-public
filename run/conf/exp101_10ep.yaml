defaults:
  - exp101
  - _self_

exp_name: exp101_10ep
description: train with contrastive loss (10 epoch)

trainer:
  epochs: 10
  use_loss_weights: true
  distillation:
    teacher_exp_name: null
    teacher_seed: null
    target_forget_rate: 0.2
    target_epochs: 4
    use_loss_weights: true
  contrastive:
    lambd: 1.0
