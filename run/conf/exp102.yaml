defaults:
  - baseline_contrastive
  - _self_

exp_name: exp102
description: train with contrastive loss

trainer:
  epochs: 4
  use_loss_weights: true
  distillation:
    teacher_exp_name: null
    teacher_seed: null
    target_forget_rate: 0.0
    target_epochs: 0
    use_loss_weights: true
  contrastive:
    lambd: 1.0
