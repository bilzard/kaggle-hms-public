defaults:
  - baseline_efficientnet
  - override architecture/model/eeg_feature_processor: eeg_dual_per_channel_v2
  - _self_

exp_name: eeg023
description: eeg016 + channel mixer(transformer)

architecture:
  model:
    eeg_encoder:
      se_after_dw_conv: true
      use_channel_mixer: true
      mixer_type: transformer
      transformer_merge_type: add
    eeg_feature_processor:
      num_eeg_channels: 10
