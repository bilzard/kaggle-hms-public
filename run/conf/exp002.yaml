defaults:
  - baseline
  - _self_

exp_name: exp002
description: input all 18 channels without mean

architecture:
  model:
    adapters:
      - _target_: src.model.adapter.ResizeTransform
        scale_factor: [2.0, 8.0]
    encoder:
      name: efficientnet_b0
      in_channels: ${architecture.in_channels}
    head:
      out_channels: ${architecture.out_channels}
  in_channels: 18
  out_channels: 6
