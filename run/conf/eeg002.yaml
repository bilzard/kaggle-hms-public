defaults:
  - baseline_eeg_v1
  - _self_

exp_name: eeg002
description: eeg001 + drop-z

architecture:
  eeg_adapter:
    eeg_dual_per_channel_collator:
      drop_z: true
  eeg_feature_processor:
    eeg_channels: 8

  in_channels: 2
  input_mask: true
