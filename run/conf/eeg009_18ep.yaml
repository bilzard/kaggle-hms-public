defaults:
  - baseline_eeg_net_wo_cm
  - _self_

exp_name: eeg009_18ep
description: EEGNet w/o channel mixing (stem_kernel=3)

architecture:
  model:
    eeg_encoder:
      stem_temporal_kernel_size: 3

trainer:
  epochs: 18
  weight_decay: 0.001
