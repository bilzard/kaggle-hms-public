defaults:
  - baseline_eeg_net_wo_cm
  - _self_

exp_name: eeg010_18ep
description: EEGNet w/o channel mixing (stem_kernel=65)

architecture:
  model:
    eeg_encoder:
      stem_temporal_kernel_size: 65

trainer:
  epochs: 18
  weight_decay: 0.001
