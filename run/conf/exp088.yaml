defaults:
  - baseline_v4
  - _self_

exp_name: exp088
description: exp068 + add weighted mean instead of z

architecture:
  model:
    adapters:
      - _target_: src.model.adapter.ConstantNormalizer
      - _target_: src.model.adapter.DualCanvasAggregatorWithWeightedMean
      - _target_: src.model.adapter.ResizeTransform
        scale_factor: [0.5, 1.0]
  in_channels: 2
  input_mask: true
  recover_dual: true
  lr_mapping_type: max-min

trainer:
  duration: 2048
