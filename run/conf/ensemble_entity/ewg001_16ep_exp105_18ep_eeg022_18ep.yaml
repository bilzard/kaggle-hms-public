name: ewg001_16ep_exp105_18ep_eeg022_18ep
ensemble_seeds: [0, 1, 2]
experiments:
  - exp_name: ewg001_16ep
    folds:
      - split: 0
        seeds: [0]
      - split: 1
        seeds: [0]
      - split: 2
        seeds: [0]
  - exp_name: exp105_18ep
    folds:
      - split: 0
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 1
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 2
        seeds: ${ensemble_entity.ensemble_seeds}
  - exp_name: eeg022_18ep
    folds:
      - split: 0
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 1
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 2
        seeds: ${ensemble_entity.ensemble_seeds}
