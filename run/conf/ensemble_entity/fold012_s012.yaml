name: ???
ensemble_seeds: [0, 1, 2]
experiments:
  - exp_name: ${ensemble_entity.name}
    folds:
      - split: 0
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 1
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 2
        seeds: ${ensemble_entity.ensemble_seeds}
