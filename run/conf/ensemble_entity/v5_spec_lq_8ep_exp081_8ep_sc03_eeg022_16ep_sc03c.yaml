name: v5_spec_lq_8ep_exp081_8ep_sc03_eeg022_16ep_sc03c
ensemble_seeds: [0, 1, 2]
experiments:
  - exp_name: v5_spec_lq_8ep
    folds:
      - split: 0
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 1
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 2
        seeds: ${ensemble_entity.ensemble_seeds}
  - exp_name: exp081_8ep_sc03
    folds:
      - split: 0
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 1
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 2
        seeds: ${ensemble_entity.ensemble_seeds}
  - exp_name: eeg022_16ep_sc03c
    folds:
      - split: 0
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 1
        seeds: ${ensemble_entity.ensemble_seeds}
      - split: 2
        seeds: ${ensemble_entity.ensemble_seeds}
