defaults:
  - baseline
  - override architecture/model/head: label_aligned_head_v2
  - _self_

exp_name: exp017
description: stack 18 channels to filter bank domain

architecture:
  model:
    adapters:
      - _target_: src.model.adapter.FlatTilingAggregator
      - _target_: src.model.adapter.ResizeTransform
        scale_factor: [2.0, 8.0] # 256 x 256
    encoder:
      grad_checkpointing: true
  in_channels: 1
